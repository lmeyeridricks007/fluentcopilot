/**
 * Re-exports the Skill-System recommendation engine for stable import paths (`skillProfileMerge`, services).
 */
export { buildSkillDrivenRecommendationPlan, buildSkillRecommendationsBundle } from './skillRecommendationEngine'

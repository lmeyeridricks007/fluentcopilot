/**
 * Final report composition: scenario blocks, premium scoring, session insights, diagnostics.
 *
 * Full composition remains in {@link ./liveSessionEvaluationOrchestrator.ts} so the stored
 * `LiveSessionEvaluation` JSON and the FluentCopilot report UI stay unchanged.
 */
export type SpeakLiveReportComposeDiagnostics = {
  pipelineVersion: 1
  composedAtIso: string
}

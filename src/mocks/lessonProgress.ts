/**
 * Per-lesson progress rows — demo-data scenario (localStorage demoScenario).
 */

import type { DemoLessonProgress } from '@/demo-data'
import { DEMO_LESSON_PROGRESS } from '@/demo-data'

export const MOCK_LESSON_PROGRESS: DemoLessonProgress[] = DEMO_LESSON_PROGRESS

/**
 * Progress summary — backed by demo-data (happy-path by default).
 */

import type { DemoProgressSummary } from '@/demo-data'
import { DEMO_PROGRESS } from '@/demo-data'

export type ProgressSummary = DemoProgressSummary

export const MOCK_PROGRESS: ProgressSummary = DEMO_PROGRESS

/**
 * Primary practice entry: mode picker (guided / semi-guided / free) with catalog rules.
 */
export function getPracticeScenarioHref(scenarioId: string): string {
  return `/app/practice/scenario/${scenarioId}`
}

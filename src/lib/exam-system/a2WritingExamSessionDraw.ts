/**
 * Official-style A2 Schrijven simulation: four assignments in one ~40-minute run (same count as the real exam).
 * `sessionSeed` is kept for API stability; task *content* still varies by seed via the bank draw.
 */
export function pickA2WritingSimulationTaskCount(_sessionSeed: string): number {
  return 4
}

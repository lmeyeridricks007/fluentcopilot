/**
 * Presenter lives in `./types` as `PracticeFeedbackPresenterModel`.
 * This file marks the boundary for UI adapters (keep imports stable).
 */
export type { PracticeFeedbackPresenterModel, PracticeFeedbackBuildResult } from '@/lib/practice-feedback/types'

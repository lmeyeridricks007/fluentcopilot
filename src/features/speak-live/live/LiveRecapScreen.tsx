/**
 * Canonical name for the post–Speak Live recap surface.
 * Implementation lives in {@link ../SpeakLiveSessionRecapView}.
 */
export { SpeakLiveSessionRecapView as LiveRecapScreen } from '../SpeakLiveSessionRecapView'

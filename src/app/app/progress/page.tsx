'use client'

import { ProgressPage } from '@/features/progress/ProgressPage'

/** Full progress surface — also embedded under Coach (`/app/coach?tab=progress`) for tab IA. */
export default function Page() {
  return <ProgressPage />
}
